# File Count

## Building

Build with
```bash
cargo build --release
```
And the executable will be in `target/release/filec`.

## Usage

```bash
filec [directory]
```

And it should output:
```bash
Files: x
Folders: y
Symbolic Links: z
```